name: linux-x64

on:
  workflow_dispatch:

jobs:
  linux-x64-ecpprog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          repository: 'yosyshq/oss-cad-suite-build'
      - name: Cache sources
        id: cache-sources
        uses: actions/cache@v2
        with:
          path: _sources
          key: cache-sources-ecpprog
      - name: Download previous build
        run: |
          URL="https://github.com/mmicko/test-ci/releases/download/bucket-linux-x64/linux-x64-ecpprog.tgz"
          if wget --spider "${URL}" 2>/dev/null; then
              wget -qO- "${URL}" | tar xvfz -
          else
              echo "Previous version not found in bucket"
          fi
      - name: Build
        run: ./builder.py build --arch=linux-x64 --target=ecpprog --single --tar
      - uses: ncipollo/release-action@v1
        if: hashFiles('linux-x64-ecpprog.tgz') != ''
        with:
          allowUpdates: True
          prerelease: True
          omitBody: True
          omitBodyDuringUpdate: True
          omitNameDuringUpdate: True
          tag: bucket
          artifacts: "linux-x64-ecpprog.tgz"
          token: ${{ secrets.GITHUB_TOKEN }}
